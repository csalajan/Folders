const initialState = {
    selectedNote: null,
    displayNotes: [],
};

const NoteReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default NoteReducer;